import React, { Component } from "react";
import { Header } from "../components/Header";

export const Search = () => (
  <div>
    <Header title="Advance Search" />
    <h2>Search function here</h2>
  </div>
);
